{
  "_comment": "Main experiment: combined feature (4 rotation groups) and label (Dirichlet 0.5) heterogeneity on full CIFAR-10. This is the setting the paper title claims and the one the headline table should report. Run it on a GPU, and run it for at least 3 seeds.",
  "num_clients": 40,
  "alpha": 0.5,
  "rotation_groups": [0, 1, 2, 3],
  "num_clusters": 4,
  "cluster_signal": "act_stats",
  "pca_dim": 32,
  "warmup_fedavg_rounds": 1,
  "warmup_local_epochs": 2,
  "warmup_act_batches": 8,
  "rounds": 100,
  "fraction": 0.25,
  "local_epochs": 2,
  "eval_every": 10,
  "batch_size": 64,
  "lr": 0.01,
  "combiners": ["uniform", "beta", "gate", "mlp"],
  "dense_rounds": 5,
  "dense_fraction": 0.25,
  "run_fedavg": true,
  "subset_analysis": true,
  "seed": 42,
  "output_dir": "./hefl/results/rotation_dirichlet_seed42"
}
