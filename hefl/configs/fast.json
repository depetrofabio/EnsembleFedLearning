{
  "_comment": "Fast end-to-end run (~40 min on Apple MPS). Exploits the act_stats finding: that signal needs no warmup training at all, so warmup_fedavg_rounds=0 and no delta signals are collected. Subsampled, so only the ORDERING of the table is meaningful.",
  "num_clients": 12,
  "alpha": 0.5,
  "rotation_groups": [0, 1, 2, 3],
  "subsample": 8000,
  "num_clusters": 4,
  "cluster_signal": "act_stats",
  "pca_dim": 8,
  "warmup_signals": ["act_stats"],
  "warmup_fedavg_rounds": 0,
  "warmup_act_batches": 8,
  "rounds": 15,
  "fraction": 0.5,
  "local_epochs": 1,
  "eval_every": 5,
  "batch_size": 64,
  "lr": 0.02,
  "combiners": ["uniform", "beta", "gate", "mlp"],
  "dense_rounds": 3,
  "dense_fraction": 0.5,
  "dense_max_batches": 8,
  "run_fedavg": true,
  "subset_analysis": false,
  "output_dir": "./hefl/results/fast"
}
